# Collections Agent — pinned dependencies
# Install: pip install -r requirements.txt

# Core
pyyaml==6.0.2
python-dotenv==1.0.1

# Google Gemini (LLM for email classification)
google-generativeai==0.8.5

# Testing
pytest==8.3.5
pytest-cov==6.2.1
